{
  "name": "loremipsum",
  "version": "1.0.0",
  "description": "== lorem ipsum wp theme  ==",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "node-sass --watch sass -o .",
    "scss:minify": "node-sass --watch sass -o . --output-style compressed",
    "auto-prefix": "npx postcss *.css --use autoprefixer -d build/ --config ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.13.1"
  },
  "devDependencies": {
    "autoprefixer": "^9.4.9",
    "postcss-cli": "^6.1.2"
  }
}
